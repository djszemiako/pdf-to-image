[project]
name = "pdf_to_images"
version = "0.3.0"
description = "Converts a PDF into a JPEG image-per-page."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "click>=8.1.8",
    "pymupdf>=1.25.4",
]
